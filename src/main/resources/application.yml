spring:
  application:
    name: screenmatch
  datasource:
    url: jdbc:postgresql://localhost/alura_series
    username: meuusuario
    password: senhaforte
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    format-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080